package com.pre.capturadatos.ui.theme

import androidx.compose.ui.graphics.Color

// --- PALETA AZUL (MODO CLARO) ---
val BrandBlue = Color(0xFF0D47A1)     // Un azul corporativo oscuro (para botones, títulos)
val BrandBlueLight = Color(0xFF1976D2) // Un azul un poco más claro (opcional)
val NeutralGray = Color(0xFFBDBDBD)   // Un gris medio (para bordes, iconos inactivos)
val BackgroundWhite = Color(0xFFFFFFFF) // Blanco puro para el fondo
val TextBlack = Color(0xFF212121)     // Negro o gris muy oscuro para el texto

// --- PALETA AZUL (MODO OSCURO) ---
val BrandBlueDark = Color(0xFF42A5F5)   // Un azul más brillante para modo oscuro
val BackgroundDark = Color(0xFF121212)  // Fondo oscuro estándar
val SurfaceDark = Color(0xFF1E1E1E)     // Superficie un poco más clara (para tarjetas)
val TextWhite = Color(0xFFE0E0E0)       // Blanco (no puro) para texto en modo oscuro